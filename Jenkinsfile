pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the project...'
                // Here you add commands to compile or build your code
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests...'
                // Here you add commands to run tests
            }
        }
    }
}
